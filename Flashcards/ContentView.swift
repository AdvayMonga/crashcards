import SwiftUI

// Placeholder shell — replaced by RootView once the UI is wired up.
struct ContentView: View {
    var body: some View {
        Text("Flashcards")
            .font(.largeTitle.bold())
    }
}
